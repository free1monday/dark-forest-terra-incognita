# Root helpers (docker-compose / local)
JWT_SECRET=change-me-in-production
CLIENT_ORIGIN=http://localhost:8080
ADMIN_SECRET=

# Postgres (also set in server/.env for local API)
DATABASE_URL=postgresql://df:df@127.0.0.1:5432/darkforest?schema=public

# One-time DB bootstrap on Vercel: GET /api/init-db?secret=...
# GET /api/drop-init?secret=... wipes public schema then migrate deploy
INIT_DB_SECRET="jRR0pSETqrMcfGTQp02fqa_XZ9eNUM5xti01yuu2oMA"
