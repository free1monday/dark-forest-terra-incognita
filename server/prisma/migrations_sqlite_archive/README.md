# SQLite migration history (archived)

These migrations were used during Stages 1–9 **local SQLite** development.

Active provider is **PostgreSQL** (`prisma/migrations/` + `migration_lock.toml`).

Do not run these against Postgres. Kept for historical reference only.
